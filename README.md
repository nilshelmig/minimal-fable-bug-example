# Minimal project for fable build error

## Setup
- run `yarn install`
- run `.paket/paket.exe restore`
- change directory to `WebUI.Fable`
- run `dotnet restore`
- go to repository root directory
- run `yarn run build`